const apiProtocol = "https";

const apiPort = "3000";

const apiPath = "";

const apiVersion = "v1";

const apiHostAuth = "";

const pageUrl = "";

const apiUrl = "localhost";

export {
  apiUrl,
  apiProtocol,
  apiPort,
  apiPath,
  apiVersion,
  apiHostAuth,
  pageUrl,
};
